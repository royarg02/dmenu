diff --git a/dmenu.c b/dmenu.c
index 709651a..1994f6c 100644
--- a/dmenu.c
+++ b/dmenu.c
@@ -204,7 +204,7 @@ drawmenu(void)
 	if (lines > 0) {
 		/* draw vertical list */
 		for (item = curr; item != next; item = item->right)
-			drawitem(item, 0, y += bh, mw - x);
+			drawitem(item, 0, y += bh, mw);
 	} else if (matches) {
 		/* draw horizontal list */
 		x += inputw;
